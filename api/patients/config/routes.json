{
  "routes": [
    {
      "method": "GET",
      "path": "/patients",
      "handler": "Patients.find",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/patients/count",
      "handler": "Patients.count",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/patients/:_id",
      "handler": "Patients.findOne",
      "config": {
        "policies": []
      }
    },
    {
      "method": "POST",
      "path": "/patients",
      "handler": "Patients.create",
      "config": {
        "policies": []
      }
    },
    {
      "method": "PUT",
      "path": "/patients/:_id",
      "handler": "Patients.update",
      "config": {
        "policies": []
      }
    },
    {
      "method": "DELETE",
      "path": "/patients/:_id",
      "handler": "Patients.destroy",
      "config": {
        "policies": []
      }
    }
  ]
}
